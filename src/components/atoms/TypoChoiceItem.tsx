export const TypoChoiceItem = ({ className }: { className?: string }) => {
  return (
    <span
      className={
        className ? `typo-choice-item ${className}` : 'typo-choice-item'
      }
    >
      Aa
    </span>
  )
}
